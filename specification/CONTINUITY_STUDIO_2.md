# Continuity Studio 2 — Authoritative Production Specification

## Product contract

Continuity Studio 2 is a chat-first, project-based, continuity-aware, provider-independent movie production system. The structured database state is the sole authority for the movie. Chat, filenames, generated-image text, provider output, and historical prompts are evidence and interaction surfaces; none may override current approved structured state.

Continuity Studio creates no separate audio asset workflow. A selected audiovisual provider such as MiniMax H3 or Seedance generates requested spoken dialogue, ambience, effects, music, and silence inside each video. The Studio owns the scenario, script, exact dialogue, numbered speaker binding, pronunciation, language and dialect, timing, actions, environment, continuity state, reference order, restrictions, and provider-specific compiled request.

## Chat product experience

The filmmaking conversation is the primary interface. A local or desktop Studio opens directly to its branded sidebar, projects, chat, composer, and attachment controls without a second Continuity Studio or OpenAI sign-in gate when the local Codex environment is already authorized. Codex is the supported local orchestration and reasoning brain through its app-server, CLI/SDK, or configured local host bridge; the database-backed structured project engine remains the authority that validates and applies changes. Provider credentials belong in setup or settings, never on the normal Studio home screen.

Story, Script, Scenario, World Bible, Film Bible, Sequence Plan, and Seedance Prompt results appear as clean expandable chat documents. Every one supports direct clean-content Copy, readable text Download, Edit-through-chat, and Regenerate actions. Visual and video results expose View and Download only when real media exists. Natural commands such as `Copy Sequence 4`, `Download the story`, `Create all required assets`, `Show Sequence 6`, and `Copy the Seedance prompt` work without forcing page navigation.

The composer accepts text, multiple attachments in one message, later attachments, drag and drop, pasted images, file selection, previews, removal, and send. It never accepts audio into a standalone production library.

### Codex reasoning boundary

For a new idea, the local Studio sends the complete idea to the authorized Codex app-server through a loopback-only host. Codex returns one schema-constrained movie blueprint containing story structure, genre and subgenre, world and film rules, comprehensive visual asset discovery, and sequence-specific dependencies. It must reason about the entire story instead of relying on a finite keyword list. For an existing project, Codex receives a compact current-state view and returns one canonical production command. The host is read-only, uses no Studio credentials, and cannot write project state. The structured engine validates legality, numbering, approval, provider boundaries, and revisions before D1 records any change. If the local host is unavailable, the deterministic interpreter remains an explicitly labeled fallback.

Automatic Production, Master Approval, and Manual Approval are the three approval behaviors. Automatic Production is the simplest default and advances routine non-paid preparation while showing results in chat. Master Approval records one approval for the complete non-paid plan. Manual Approval waits at individual stages. None authorizes paid image or video work by default. `Continue` performs or explains the next non-paid step and stops before an expensive provider call.

## Project state machine

The only forward lifecycle is:

1. Story Draft
2. Story Approved
3. Assets Pending
4. Assets Approved
5. Sequences Ready
6. Production Started
7. Final Review
8. Completed

The Studio derives this state from approved structured data and records every transition. It rejects illegal jumps. A changed prerequisite may move the project backward to the last legal state without deleting approved history.

## Data authority and schema

Data schema version 5 is current. Every archive records the version that created it, all migrations applied, and the supported version range. Versions 1–5 must migrate through normalization before use. D1 stores authoritative structured state, revisions, transaction guards, version histories, pins, lifecycle state, generation idempotency, checksums, import reviews, recovery points, verification records, workflow pins, storyboard panels, stable references and schedules, provider translations, local runtime jobs, candidates, continuity handoffs, and generation provenance. R2 stores original and generated bytes. Browser storage is never authoritative.

Every mutation is optimistic and atomic. A stale revision fails without partial writes. Every major operation—and currently every persisted mutation—creates a recovery point before changing the project.

## Permanent assets and files

One movie has one flat asset folder with no subfolders. Every visual production asset shares one permanent project-wide number. A regeneration keeps its number. A retired asset is never deleted and its number is reserved forever. Existing sequence links remain historical evidence. Orphan detection identifies active assets no longer connected to active sequences.

Portable filenames are deterministic: `NNN_NAME_GENERATED.png`. Names are normalized for Windows, macOS, ZIP, Unicode, length, and reserved device names while retaining the permanent numeric prefix. The export identity combines the project ID with the title so projects sharing a title cannot collide.

Uploaded photographs and other source references have internal reference identifiers and do not consume production asset numbers. Multiple photographs of one person contribute to one locked character identity. One Master Character Sheet is one coherent composite image, one file, and one numbered production asset; front, profile, three-quarter, full-body, rear, face-detail, or other views are panels inside that sheet. The same counting rule applies to a composed costume, environment, location, prop, creature, vehicle, mechanical, transformation, or damage sheet when its views belong to the same stable production asset or state. Panels never increase the asset count. Genuinely different states or production elements remain separately discovered numbered assets.

## Approved decision pins and inference

The user can pin character identity, costume, location, dialogue, camera rules, asset versions, and sequence versions through chat. A pin is an approved structured record. No inference, regeneration, provider result, or story edit may change it until the user explicitly releases it.

Routine filmmaking choices may be inferred. Every inferred value records its rationale, confidence, source, and editability. Inference never becomes a permanent lock without explicit user approval or an existing project rule.

## References, warnings, and confidence

Automatic reference roles remain editable. A user can restrict a reference to one role or exclude traits such as hair. Bindings store confidence and whether they were inferred. Low-confidence bindings require review before expensive generation.

Warnings have two levels. A Blocker prevents the affected expensive or impossible operation. A Recommendation remains visible but does not stop normal work.

## Generation and providers

Creating a story, Bible, manifest, sheet brief, scenario, script, sequence, reference map, continuity package, or Seedance prompt never starts video. Video preparation begins only after an explicit command equivalent to `Generate Sequence 1`, `Create this video`, or `Send this to Seedance`; paid submission still receives the visible provider/model/duration/resolution/cost confirmation unless the user explicitly configured automatic paid generation. A Regenerate document or sequence command can never be parsed as Generate video.

Every generation request is rebuilt from current approved structured data. Historical prompt text is never submitted blindly. Entering generation creates an immutable per-attempt freeze containing scenario, exact dialogue, upload order, compiled prompt, continuity, provider, exact model version, capability revision, settings, data version, and structured-state hash.

Provider adapters implement capability refresh, deterministic translation, idempotent submission, status polling, and cancellation. Controlled queues enforce per-provider concurrency. One immutable request fingerprint maps to one job and one submission token. Timeouts poll the same provider request; they do not silently create another paid generation.

Every approved result records exact provider/model versions, settings, references, asset versions, scenario/dialogue versions, immutable snapshot, checksum-backed media source, and final source-map entry.

An explicit command to create a prepared composite visual sheet may submit exactly one image request when a server-side provider is configured. Character generation sends all linked PNG, JPEG, or WebP likeness references to the image-edit endpoint as evidence for one identity and requests one composite board. Other prepared sheets may use linked references or a text-only generation. The generated PNG is stored in R2; D1 records its reference metadata, checksum, generation job, result, provider request ID, asset version, recovery snapshot, and change log. The asset keeps its permanent number and `NNN_NAME_GENERATED.png` filename across regeneration. A successful result remains `Needs Review` until approved. Missing keys, unsupported source types, missing bytes, stale revisions, and provider errors leave the prepared brief and existing media unchanged.

## Import, storage, recovery, and verification

Full archives restore structured state, chat, versions, pins, approvals, provenance, jobs, recovery, and original bytes. Old folders are analyzed before creation. Filenames and media produce a confidence-rated mapping for assets, references, sequence videos, prompts, and supporting data. The user approves that mapping before the project is created.

Storage management reports originals, previews, generated files, and cleanup candidates. Safe cleanup may remove only unused derived previews and failed unapproved generation media. Originals, approved assets, approved sequence results, final sources, provenance, and recovery data are protected.

Important media uses SHA-256. Reads and exports detect missing, corrupt, replaced, and duplicate bytes. Full archives and flat-asset ZIPs contain a deterministic manifest and are reopened and checksum-verified before download.

The chat command `repair this project` inspects relationships, files, numbering, references, approvals, dependencies, and continuity. It repairs only safe derived inconsistencies and reports ambiguous or missing source data for user review.

## Advanced control surface

Normal work remains in chat. An optional Advanced Control screen shows the lifecycle, dependency graph, blockers versus recommendations, inference confidence, pins, retired/orphan assets, version comparisons, and storage cleanup. Settings contains a deliberate secondary path into this screen; it is not part of primary filmmaking navigation.

## Local AI production runtime

The Windows-first local runtime is a second production layer beneath the chat-first Studio, not a competing interface and not a replacement source of truth. The Studio owns intent, approved decisions, stable references, schedules, immutable attempts, review decisions, and continuity. ComfyUI is a loopback-only execution backend. The normal filmmaker never has to edit its graph, copy node identifiers, or manage its queue directly.

The runtime is registry-driven. Component, model, and workflow manifests declare exact repository URLs, pinned commits or versions, expected checksums, upstream licenses, model paths, optional versus required status, and compatible hardware profiles. Models remain outside Git and never download implicitly. Installation, update, model verification, workflow verification, and generation are separate visible operations with progress, recovery guidance, and precise blockers. Missing CUDA, VRAM, nodes, weights, checksums, or licenses may not be disguised as readiness.

Imported workflows are immutable source evidence. Each receives a checksum, license status, source path, semantic role map, compatibility findings, and a Studio-owned execution adapter. The adapter resolves nodes by class, title, ancestry, and required inputs instead of depending on fragile serialized node numbers. The supplied Krea and MiniMax workflow remains external because its license is unspecified. Its serialized `REF2VA` loader selects an FL2VA checkpoint; that mismatch is blocking for H3 Ref2VA execution until a genuine validated Ref2VA checkpoint is selected. It does not block an independently valid Krea storyboard target.

Every sequence stores one provider-neutral cinematic intention and deterministic translations. MiniMax H3 translations support T2VA, I2VA, FL2VA, L2VA, and Ref2VA prompt documents with subject definitions, summary, retention analysis, detailed description, overall soundscape, non-diegetic music, dialogue, reference mapping, and mode-specific context. Studio reference tags such as `@hero_face`, `@location_main`, and `@storyboard` remain stable; provider-native Picture, Video, and Audio numbering exists only in the immutable attempt. Per-sequence seed, steps, sampler, scheduler, LoRAs, width, height, candidate count, video context, audio context, and continuation mode are advanced settings frozen with the attempt. Only a separately validated graph may execute each mode; a compilable prompt is not proof of executable runtime support.

The Krea storyboard is a real project object with stable panel labels, prompts, generation lineage, generated-file evidence, and approval state. A scoped edit or regeneration changes only the named panel, preserves its prior lineage, clears its generated file, and returns it to Draft. A panel or candidate cannot be approved until real media is attached and awaiting review. The runtime exposes competing candidates without overwriting them and supports Approve, Edit and Retry, Generate Another Candidate, Approve and Stop, and Reject. Rejection preserves the immutable snapshot, settings, output, and provenance.

Approved sequence candidates create immutable continuity handoffs. A handoff can carry the approved video, extracted ending frames, continuation frames, ending latent when supported, video context, native audio context when supported, character positions, wardrobe, held objects, environment state, motion, screen direction, and next-opening expectation. Sequence 1 starts independently. Later sequences consume the latest approved prior handoff and the current approved structured state. Pause, cancel, retry, resume, close, reopen, and crash recovery never silently create a different attempt.

The local runtime accepts no arbitrary filesystem path or remote URL from browser state. Before ComfyUI submission it resolves only same-project Studio files or same-project completed runtime outputs, verifies identity and content type, enforces an image-size limit, and stages safe generated filenames through ComfyUI's upload API. Runtime output URLs are read-only views over files recorded for known completed jobs. Terminal result synchronization is localhost-only and writes candidate, storyboard, job, and provenance state through the same revision-checked database mutation boundary as every other Studio action.

The primary local UI exposes Movie Workspace and Local AI Engine views. Movie Workspace contains sequence tabs, active stable references, provider prompt inspection, storyboard review, immutable prepared jobs, candidate comparison, and continuity handoff status. Local AI Engine reports actual GPU, Python, FFmpeg, ComfyUI, component, model, checksum, workflow, Krea-target, and H3-target readiness. Hosted Sites builds preserve planning and project workflows but cannot claim access to the user's local GPU or Codex session.

## Acceptance contract

Every release must cover:

- backward migration from every supported data version;
- legal and illegal state transitions;
- pin enforcement and explicit release;
- retirement and orphan detection without number reuse;
- deterministic portable naming and title-collision isolation;
- checksum, duplicate, corruption, missing-file, and archive-manifest verification;
- import mapping review before old-folder creation;
- generation queue limits, idempotent timeout/retry, and exact model pinning;
- prompt regression for dialogue speakers, numbered references, continuity, scene details, role overrides, and restrictions;
- end-to-end idea, approval, assets, bindings, planning, dialogue, generation freeze, regeneration, restart, export, restore, and final assembly;
- enforcement of the no-audio-asset policy.
- UI-level blank-project qualification through the actual chat, including multi-reference previews and upload, one-identity mapping, one composite sheet number, document Copy/Download actions, safe `Continue`, natural prompt control, explicit-only video, and close/reopen restoration without direct database seeding or fixture media counted as production success.
- a live Codex app-server qualification proving schema-constrained blueprint creation and canonical command interpretation through the already-authorized local Codex session;
- provider adapter tests for multi-reference image edits, text-only generation, missing-key behavior, durable generated bytes, permanent-number retention, and version-safe regeneration.
